---
name: courses-openai-guidance
description: Refresh OpenAI model, Codex, skill or prompt guidance used by this repository; not ordinary UI implementation.
---

# OpenAI guidance refresh

Project-authored workflow informed by the official sources in `docs/astra/sources.json`; not a copy or installation of the official openai-docs skill.

Determine the actual requested surface: task prompt, durable repository rule, reusable skill, client configuration, live docs connection or product API integration. Preserve an explicitly requested model target. For latest/current model questions, reopen the official latest-model guide and its linked migration guidance; do not turn the branch name astra-pro into an API slug.

Search the requested topic with the available official documentation tools, then fetch the relevant page; use official web documentation as fallback. For an installed official skill, inspect its actual files and prerequisites before invoking its helpers; this repository does not bundle those upstream scripts. Use connected GitHub/computer tools for private live state. A fetched skill is not permission to install, elevate, change credentials or overwrite global settings. The current user assignment takes precedence over workflow suggestions; preserve permissions already granted for that assignment.

Compare the dated register with current source content. Identify a concrete changed rule, affected project files and verification. Keep descriptions narrow, durable entry points concise and details in task-specific documents. Do not copy an entire catalog into mandatory context or use generic frontend defaults against exact Apple references.

Update source IDs, URLs, dates, reviewed scope and upstream pin where relevant. Distinguish reviewed content from indexed links, structural validation from behavioral evaluation, and files in Git from skills actually loaded in a client. Preserve old evidence/model fixtures unless the assigned migration genuinely requires them to change.

Run document checks and the appropriate affected tests. Report unavailable sources or capabilities explicitly. No phase transition, product AI feature, paid provisioning or acceptance is authorized by this workflow.

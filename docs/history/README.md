# Historical documentation — not active instructions

`pre-astra-2026-09-12/` preserves the committed documentation at `289c049cc3e198baa762cd89850f744e80e99ff2`, before the owner-authorized Astra rewrite. The `docs/` subtree reuses the original Git tree `3a07bc4fb09b409fa069f68b0a826edccfeceaff`; its files, checklist snapshots, metrics and reviews are byte-identical to that checkpoint. Root/app entry-point snapshots are retained alongside it.

These files explain previous decisions and evidence. They contain superseded main-only, course-first and new-`web/` directions and historical server/test claims. Do not load them as current agent instructions or use their copied checklist as a second live backlog. Relative links inside snapshots preserve their historical text and may refer to the old layout; use the current documentation map for navigation.

Active authority is the root `AGENTS.md`, current `docs/decisions.md`, current handoff and the single live `docs/tasks.md`. The original reference archive remains in `apple-music-clone/reference/`, unchanged and not duplicated here. Do not rewrite historical evidence to match later implementation.

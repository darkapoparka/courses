# Sidebar correction review - 2026-09-12

## Outcome: bounded correction, not visual acceptance

The sidebar is not a signed-off 1:1 reconstruction. No MATCH or FLOW checkbox was advanced. The retained changes restore the primary row positions at the 1440 x 903 viewport, preserve the text anchor at x=52 while giving navigation symbols their own optical bounds, remove the incorrect arrow from Recently Added, and reconstruct the missing outer radio wave and several mismatched navigation symbols. The scrolled Home pane's overall warm tint was reduced; its artwork/material reconstruction remains partial.

Changed implementation: `app/globals.css`, `app/reference-fidelity.css`, `components/apple-music-app.tsx`, and `components/music-sidebar-icons.tsx`. The existing `sidebar-and-rail-containment` regression in `scripts/browser_fidelity_regressions.py` now asserts the 232px pane, Search/Home/New/Radio row positions (76/114/152/190), 19px SVG canvases, x=52 labels, and a compiled standard backdrop filter. These geometry assertions do not assert font or visual parity.

Global type-size, active-colour, pane-background and shadow experiments were rejected after cross-family comparisons. Do not reinstate them merely because the first New screenshot improves. Removing the scrolled gradients without fixing their underlying artwork also regressed the reference comparisons and was rejected.

## Verified working-tree candidate

Implementation SHA-256: `1ceac6bcf17847815661a298be3efac54827cf69b3e47cf479de892e2425078a`.

QA tooling SHA-256: `5e77f595aa31e4e4f41d65336e617a5598b34ca8e2b6c354ec8b619b5161c7d3`.

This identity includes the parallel library-control and cover-integrity batch in the same working tree. It is not a claim that the sidebar-only commit, without that other implementation, passed the combined suite.

The matching rebuilt production listener on port 6432 used build ID `VHc1Pa-LxOrZt4R7jL7Rt`. Its build-start/build-complete implementation identities matched the browser run and the inspected working tree. The complete run passed 159 desktop captures, 5 responsive samples, 218 route checks and 48 interaction regressions, with no failures and stable implementation/tooling identities. All 159 exact-size states were compared.

A separate production sidebar check passed 9 desktop captures and 11 interaction regressions: sidebar/rail containment, Home artwork persistence through real controls, pointer/keyboard menu modality, and all eight recorded library journeys. Its source remained stable. This is regression coverage, not completed recorded-flow acceptance.

## Evidence (local, deliberately uncommitted)

- Full run: `apple-music-clone/.parity-evidence/controls-20260912-074455/production-integrity/results.json`.
- Full source/render/difference gallery and metrics: `apple-music-clone/.parity-evidence/controls-20260912-074455/production-integrity-comparison/`.
- Separate sidebar production run: `apple-music-clone/.parity-evidence/sidebar-20260912-074616/geometry-production/focused/results.json`.
- Readable sidebar review images: `apple-music-clone/.parity-evidence/sidebar-20260912-074616/geometry-review/`. These crops supplement the unmasked full-viewport comparisons; they are not acceptance evidence on their own.

## Visual counter-evidence

Against the preceding complete Windows production comparison, the combined candidate has 105 lower, 2 unchanged and 52 higher over-20 residuals. Mean absolute error is lower in 98, unchanged in 1 and higher in 60 states. This is mixed evidence, not an all-screen fidelity pass.

| Original | Previous over-20 | Current over-20 | Previous MAE | Current MAE |
| --- | ---: | ---: | ---: | ---: |
| New `e72be564` | 6.77249% | 6.75457% | 5.75820 | 5.72602 |
| Scrolled Home `d5173715` | 6.04882% | 6.07981% | 4.97798 | 4.72765 |
| Alpha `54b01eab` | 9.88564% | 9.86588% | 6.97723 | 6.95044 |

Home's lower mean error does not cancel its higher threshold residual. The largest threshold increases are `e379e3fe` (+0.06499 percentage points), `ffc18eb8` (+0.05654) and `3728aa07` (+0.05539); their 904px typography/icon alignment remains unresolved. Do not add arbitrary viewport-specific offsets to hide these differences.

Remaining sidebar defects include the actual rendered font and optical text metrics; some symbol strokes and avatar/brand details; the plain pane and selected-row colour differences between captures; the incorrect Alex station animation frame beneath Home glass; and Alpha's partial/incorrect preceding artwork, compensating gradients, selected-row opacity and source-dependent material. Replacing the frozen archive, painting a screenshot over the UI, or calling a green browser run a visual match would not fix those defects.

## Execution cautions

Another session changed this same main checkout during the audit. Existing library, context, menu, cover-integrity and workflow changes were preserved. Do not overwrite them with the earlier handoff. No reference image, dependency or generated evidence was staged as part of the sidebar correction.

The C: drive reached zero free space. One rebuild failed before compilation with npm ENOSPC. This audit used process-local TEMP/TMP and npm cache paths under `D:\courses-sidebar-audit-temp-20260912-074616` without deleting prior work. The successful compilation was source-stable. Its first attempted extra listener then lost `.next` to a concurrent build; it was not used as verification. The subsequent matched production build and its stable run above are the evidence of record.

Avoid simultaneous builds into the same `.next` directory. A source hash alone does not prove which build a listener is serving: verify the build ID, listener ownership, build-start/build-complete identities, and the browser candidate. Keep the user's port-6431 development preview running. Excluded scratch experiments and backups remain under the sidebar evidence directory; they are not accepted candidates.

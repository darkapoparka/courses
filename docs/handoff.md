# Courses — current handoff

## Latest signup-control follow-up

[Verified signup review](reference-review/2026-09-20-signup-control-chrome.md). Build `Rb2nQZf5zZtyItM1k47jY`; 159 desktop states, five responsive states, 218 routes and 98 tests, zero failures. Evidence: `.qa/evidence/signup-chrome-20260920`. MATCH remains 22/159, FLOW 3/58.

The 36-file preceding batch is published as `32ea2e1089a7a28622885c05ce914703d0c89f6a`; both workflows passed. The immediate next defect is the initial New selected-row corners/ink and player glass/underlay, confirmed by native source/render/difference review. Preserve the ATEEZ cover repair and all playback/session work.

For ordinary pushes, use the configured manager without the global selector: `git -c credential.helper= -c credential.helper=manager -c credential.interactive=never push origin main`. Do not change global credential settings or expose tokens.

The older runtime/parent details below belong to the preceding batch; recheck exact listeners and Git before reuse.


Updated 2026-09-20 for the player/Replay/release implementation checkpoint.

## Single checkout and preservation

Only checkout: `J:\courses`. Only working branch: `main`. Active app: `J:\courses\apple-music-clone`. Preserve the immutable reference archive, all evidence and unrelated projects. No cleanup, deployment or course adaptation.

## Verified implementation

Read [the latest review](reference-review/2026-09-20-player-replay-release-checkpoint.md) for exact repairs, hashes, tests, metrics, visual blockers and capture provenance. The named/playing New second release is now the pinned ATEEZ cover, not Lover. Expanded lyrics/artwork/menu, modal gestures, playback completion, profile continuity, signup and Replay work are included in the verified batch.
Optimized build: `gLF6tBPHo85e4ter92Rtl`. Full run: 159 desktop screens, five responsive screens, 218 routes and 97 interaction tests, zero failures; all 159 comparisons completed.
Evidence root: `J:\courses\apple-music-clone\.qa\evidence\publish-20260920`.
Mean MAE 5.049159017 -> 4.950875687; over-20 5.154771669% -> 4.990549245%.

## Acceptance remains incomplete

UI 159/159. MATCH 22/159. FLOW 3/58. `docs/tasks.md` is the sole acceptance ledger. Numerical improvement and green tests do not grant acceptance.

## Continue implementation

Prioritize initial New `e72be564`, which blocks multiple Search/Library flows. Preserve the distinct initial/current catalog editions and the repaired queue/Autoplay/account/history state. Fix source-backed visual and behavioral defects; do not repeat completed recovery work.
Replay still needs ambient lighting, obscured song metadata, gallery continuation, live artist-card captions and finishing of captions/player material. Expanded lyrics and account paths retain exact-state visual and session discontinuities. The latest review identifies these without inventing source content.

## Runtime and storage

Canonical development: `http://127.0.0.1:6435/` from the active checkout; listener 9112, parent project Next-dev 31392, parent 49576 absent at inspection. Focused real-control verification passed. Recheck these PIDs before touching any process.
Fresh audit used port 6437, build directory `.qa/build-publish-20260920`, listener 33000 launched from the active checkout. Stop only its exact verified project listener after finishing; never its remote-connection ancestors.
QA: `D:\courses-main-qa\audit-venv\Scripts\python.exe`; browser `D:\courses-main-qa\playwright-browsers\chromium-1234\chrome-win64\chrome.exe`.
Last measured free space: J: 17.135 GiB, D: 9.393 GiB. No deletion or cleanup was performed. Existing D-backed junctions remain intact.

## Publication

Parent commit: `fd39956cbbe33aba62a2b1d19eafa49119b15fe4`. Final commit, push and CI receipt must be checked against Git, not inferred from this pre-commit document. Both parent workflows passed; that does not certify this new batch.

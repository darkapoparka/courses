# Courses project instructions

@AGENTS.md

The root `AGENTS.md` is the canonical operating contract. Read it before acting on framework-generated instructions in the archived prototype. Start with `docs/README.md`, `docs/decisions.md` and the assigned task in `docs/tasks.md`.

Current scope is reference audit and product planning. Documentation, a backlog, or an existing framework scaffold is not authorization to implement or deploy. Preserve `apple-music-clone/` and its saved references. Use `docs/handoff.md` for the next bounded local reference-review task. Do not maintain a second product specification in this file.
